spring:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  datasource:
    driver-class-name: #org.postgresql.Driver
    username: postgres
    password: 123
    url: jdbc:postgresql://localhost:5432/jenkins_example


server:
  port: 8084